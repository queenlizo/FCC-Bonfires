function boo(bool) {
  // What is the new fad diet for ghost developers? The Boolean.
  
    if (bool == Boolean) {
        return true;
    } else if (bool === true||false){
        return true;
    } else if (bool === false){
        return true;
    } else {
        return false;
    }
}

boo(null);